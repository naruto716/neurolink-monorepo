import { createTheme } from '@mui/material';

// Create and export the theme
export const theme = createTheme({
  palette: {
    primary: {
      main: '#3f51b5',
    },
    secondary: {
      main: '#f50057',
    },
  },
}); 